name: auth
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.12.0
  flutter_secure_storage: ^10.0.0
  provider: ^6.1.5+1
  google_sign_in: ^7.2.0
  url_launcher: ^6.3.2
  otp: ^3.2.0
  flutter_launcher_icons: "^0.13.1"
  crypto: ^3.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/fingerprint.png"